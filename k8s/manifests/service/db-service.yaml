apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: development
spec:
  selector:
    app: "backend"
    db-version: "latest"
  type: ClusterIP
  ports: 
    - protocol: TCP
      port: 5432
      targetPort: 5432
