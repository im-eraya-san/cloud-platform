apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: development
spec:
  selector:
    app: "app"
    app-version: "v1"
  type: ClusterIP
  ports: 
    - protocol: TCP
      port: 8090
      targetPort: 5000
