apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
  namespace: development
  labels:
    env: development
    version: v1

spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"

